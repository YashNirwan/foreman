# Copy to .streamlit/secrets.toml locally, or paste into the Secrets box in the
# Streamlit Community Cloud app settings. Only the search tab needs this; the alert
# queue and audit views read committed analysis output and work without a key.
NVIDIA_API_KEY = "nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
